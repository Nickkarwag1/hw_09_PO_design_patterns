interface Home_page {
    CATALOG: string;
}

interface Mid_menu {
    ELECTRONICS: string;
    AUTO_MOTO: string;
    WORK_OFFICE: string;
}

interface Product {
    TABLETS: string;
    CONSOLE: string;
    CAR_BATTERY: string;
    OFFICE_CHAIRS: string;
    IP_CAMERAS: string;
}

interface Headers_titles {
    HEADERS_PRODUCT: string;
}

interface Left_side_bar {
    TABLETS_EBOOKS: string;
    VIDEOGAMES: string;
    SPARES: string;
    OFFICE_FURNITURE: string;
    VIDEO_SURVEILLANCE: string;
}

interface Titles {
    CATALOG: string;
    TABLETS: string;
    CONSOLE: string;
    CAR_BATTERY: string;
    OFFICE_CHAIRS: string;
    IP_CAMERAS: string;
}

export { Home_page, Mid_menu, Product, Headers_titles, Left_side_bar, Titles };
